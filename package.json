{
  "name": "orbi-lock-fall-phaser",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 4175 --strictPort",
    "build": "vite build",
    "test": "node --test"
  },
  "dependencies": {
    "phaser": "3.90.0"
  },
  "devDependencies": {
    "vite": "7.1.5"
  }
}
